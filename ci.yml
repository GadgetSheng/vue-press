image: vuepress:v0.0.3

pages:
  cache:
    paths:
      - node_modules/
  stage: build
  script:
  - PROJECT_PATH="/${CI_PROJECT_PATH}/" npx vuepress build -d public/ docs
  artifacts:
    paths:
    - public/
  only:
  - master